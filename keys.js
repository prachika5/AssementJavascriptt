const bike = {
    name : 'Pulser',
    model: 'N210 V2',
    Color : 'black n White',
    price : '1,75,000',
};

const count = Object.keys(bike);

const countkey =count.length;
console.log("Number of Keys: " + countkey)